apiVersion: v2
name: cert-manager
description: cert-manager and issuers for OKDP
version: 0.1.0
dependencies:
  - name: cert-manager
    version: "v1.17.1"
    repository: "https://charts.jetstack.io"
