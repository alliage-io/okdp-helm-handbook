apiVersion: v2
name: cert-manager
description: cert-manager and issuers for OKDP
version: 0.1.0
dependencies:
  - name: trust-manager
    version: "v0.16.0"
    repository: "https://charts.jetstack.io"
